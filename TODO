ADDITIONS:


CHANGES:
    move src/ads/*.jpg to img/ads/*.jpg

    standardize capitalization of Views, e.g. why lower case 'd' in detailsView, but upper case 'S' in SearchbarView

    consider renaming SearchbarView to SearchFormView, same for the *Container classNames

    consider renaming SearchbarView's topnav className, topnavs are typically small navigation bars at the top of a page similar to that found on https://kth.se/ 


FIXES:
